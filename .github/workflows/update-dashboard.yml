name: Update VISION UNIVERSE dashboard data

on:
  workflow_dispatch:
  schedule:
    - cron: "0 7 * * 6"

permissions:
  contents: write

jobs:
  update-dashboard-data:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Fetch market data
        env:
          TWELVE_DATA_API_KEY: ${{ secrets.TWELVE_DATA_API_KEY }}
        run: python scripts/dashboard/fetch_market_data.py

      - name: Calculate technical scores
        run: python scripts/dashboard/calculate_technicals.py

      - name: Verify generated-file-only scope
        run: |
          unexpected="$(git diff --name-only | grep -vE '^dashboard/data/(market_data|technical_scores)\\.json

      - name: Commit generated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add dashboard/data/market_data.json dashboard/data/technical_scores.json
          git commit -m "chore: update dashboard market data" || exit 0
          git push
 || true)"
          if [ -n "$unexpected" ]; then
            echo "Unexpected modified files:"
            echo "$unexpected"
            exit 1
          fi
          test -n "$(git diff -- dashboard/data/market_data.json dashboard/data/technical_scores.json)" || exit 0

      - name: Commit generated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add dashboard/data/market_data.json dashboard/data/technical_scores.json
          git commit -m "chore: update dashboard market data" || exit 0
          git push
